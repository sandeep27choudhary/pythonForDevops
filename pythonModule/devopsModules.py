# os module: provides a portable way of using operating system dependent functionality
import os

# platform: provides access to underlying platform's identifying data
import platform

# subprocess: allows you to spawn new processes, connect to their input/output/error pipes, and obtain their return codes
import subprocess

# sys: provides access to some variables used or maintained by the Python interpreter and to functions that interact strongly with the interpreter
import sys

# psutil: provides cross-platform functions for retrieving information on running processes and system utilization (CPU, memory, disks, network, sensors)
import psutil

# re (Regular Expression): provides support for regular expressions
import re

# scapy: a powerful interactive packet manipulation program and library
import scapy

# Requests and urllib3: libraries for making HTTP requests
import requests
import urllib3

# logging: provides a flexible framework for emitting log messages from Python programs
import logging

# getpass: prompts the user for a password without echoing
import getpass

# boto3: Amazon Web Services (AWS) SDK for Python
import boto3

# paramiko: a Python implementation of the SSH protocol for secure connections to remote machines
import paramiko

# JSON: a lightweight data interchange format inspired by JavaScript object literal syntax
import json

# PyYAML: a YAML parser and emitter for Python
import yaml

# pandas: a powerful data analysis and manipulation library for Python
import pandas

# smtplib: defines an SMTP client session object that can be used to send mail to any internet machine with an SMTP or ESMTP listener daemon
import smtplib
